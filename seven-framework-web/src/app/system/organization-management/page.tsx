'use client';

import React from 'react';
import UnifiedOrgTable from './components/UnifiedOrgTable';

/**
 * 统一的组织管理页面 - 支持组织/部门/岗位的层级展开和CRUD操作
 */
export default function OrganizationManagementPage() {
  return <UnifiedOrgTable />;
}
